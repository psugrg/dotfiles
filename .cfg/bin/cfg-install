#!/bin/bash
git clone --bare https://github.com/psugrg/dotfiles.git $HOME/.cfg
function config {
   /usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME $@
}
mkdir -p .config-backup
config checkout -f
config config status.showUntrackedFiles no
